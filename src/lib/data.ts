import type { Customer, PriceBookItem, Proposal } from '@/lib/types';

// Mock data is no longer used, but the arrays are kept to prevent import errors
// in files that will be updated to use Firestore.
export const mockCustomers: Customer[] = [];

export const mockProposals: Proposal[] = [];

export const mockPriceBook: PriceBookItem[] = [];
